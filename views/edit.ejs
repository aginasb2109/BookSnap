<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book Review</title>
    <link rel="stylesheet" href="/styles/edit.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <header class="d-flex flex-wrap justify-content-center py-3 mb-4">
        <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none fs-5 text-light mx-2">
            Edit of the Book - <%= book.title %>
        </a>
    </header>
   
    <% if (editMode) { %>
    <div class="container">
        <div class="review">
            <div class="content">
                <form action="/edit/<%= book.id %>" method="POST" id="editForm">
                    <input type="hidden" name="id" value="<%= book.id %>">
    
                    <input type="text" name="title" id="titleInput" class="form-control mb-3" value="<%= book.title %>" required>
                    <input type="text" name="author" id="authorInput" class="form-control mb-3" value="<%= book.author %>" required>
                    <input type="text" name="dateofpublish" id="dateInput" class="form-control mb-3" value="<%= book.dateofpublish %>" required>
                    <textarea name="review" id="reviewInput" class="form-control mb-3" required><%= book.review %></textarea>
                    <input type="number" name="rating" id="ratingInput" class="form-control mb-3" value="<%= book.rating %>" required min="1" max="5">
                   
    
                    <button type="submit" class="btn btn-success">Save</button>
                    
                </form>
            </div>
            <div class="img">
                <img src="<%= book.cover_url %>" alt="<%= book.title %>">
            </div>
        </div>
    </div>
    <% } %>
</body>
</html>
